# Local secrets — never commit real values.
# Copy to .env for local paid canary only after Research Lead approval.

GEMINI_API_KEY=your_key_here
GEMINI_MODEL=your_approved_model_id
POC_MAX_COST_USD=15.00
